# ── AE Labs — Caddyfile ──────────────────────────────────────────
# HTTP seulement pour l'instant — ajouter domaine pour SSL auto

:80 {
    # Bloquer /admin à tout le monde sauf ton IP
    @admin_block {
        path /admin*
        not remote_ip 62.35.181.217
    }
    respond @admin_block 403

    reverse_proxy app:3000
}

# Décommenter quand domaine disponible (SSL auto via Let's Encrypt) :
# aelabs.io {
#     reverse_proxy app:3000
# }
