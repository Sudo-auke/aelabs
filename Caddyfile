# ── EMBDX — Caddyfile ──────────────────────────────────────────

:80 {
    # Bloquer /admin à tout le monde sauf ton IP
    @admin_block {
        path /admin*
        not remote_ip 62.35.181.217
    }
    respond @admin_block 403

    reverse_proxy app:3000
}

# Analytics — stats.embdx.com (accès libre pour l'instant, ajouter IP restriction si besoin)
stats.embdx.com {
    reverse_proxy umami:3000
}
